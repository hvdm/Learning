#include <iostream>
#define LOG(x)	std::cout << x << std::endl

// The only difference between a CLASS and a STRUCT is:
// By default a CLASS everything is PRIVATE
// By default a STRUCT everything is PUBLIC


// Preferred to use struct as some kind of records that holds variables, otherwise use class
struct Vec2
{
	float x, y;
};


int main()
{




	std::cin.get();
}